let idPaciente = 5321;
let pacienteFalta = true;
const dadosPaciente ={    // isso é um objeto 
    primeiroNome: 'Rogério',
    segundoNome: 'Ferreira',
}

const emailPaciente = 'rogerioferreiraeu@gmail.com';

console.log(typeof dadosPaciente);
console.log(typeof idPaciente);
console.log(typeof idaPaciente);

// ====================================================================

const base = 5;
let height = 8;
const area = (base * height);

console.log(area);
//======================================================================

/* Crie uma costante chamada base e uma variável chamada height e atribua os seus respectivos valores: 5 e 8.
Crie uma constante chamada area e atribua a ela o resultado da multiplicação da base pela altura. Dica: lembre-se de usar sempre o console.log() para imprimir as variáveis e checar os resultados das operações!
Crie uma constante chamada perimeter e atribua a ela a soma de todos os lados do retângulo. */


let lado1 = 90;
let multiplicacao = 4; 

const perimetro = lado1 * multiplicacao;

console.log(perimetro);